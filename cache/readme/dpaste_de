dpaste_de.el
============

Emacs mode to post to dpaste.de

In your `init.el`, load the `dpaste_de.el`

    (require 'dpaste_de)

Three commands should get activated which you can use as follows

    M-x dpaste-region             ;; paste a marked region
	M-x dpaste-buffer             ;; paste the current buffer
	M-x dpaste-buffer-with-name   ;; paste a buffer with name
