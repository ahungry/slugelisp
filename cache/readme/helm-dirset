# helm-dirset

helm sources for multi directories.

## Usage

    (require 'helm-dirset)
    (defun my::helm-directories ()
      (interactive)
      (helm-other-buffer
       (helm-dirset-sources '("/path/to/dir"
                              "/path/more/to/dir2"
                              "/Users/k1low")) nil))


